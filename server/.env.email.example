# ===== HOSTINGER SMTP EMAIL CONFIGURATION (PRODUCTION) =====
# Add these to your Render.com / Heroku / Production environment variables

SMTP_HOST=smtp.hostinger.com
SMTP_PORT=465
SMTP_SECURE=true
SMTP_USER=info@garava.in              # Your Hostinger email address
SMTP_PASSWORD=your_actual_password     # Your Hostinger email password
EMAIL_FROM=info@garava.in              # Must match SMTP_USER

# Notes:
# 1. Replace SMTP_USER with your actual Hostinger email
# 2. Replace SMTP_PASSWORD with your actual email password
# 3. EMAIL_FROM should be the same as SMTP_USER for proper authentication
# 4. Keep these secure - never commit to Git!
